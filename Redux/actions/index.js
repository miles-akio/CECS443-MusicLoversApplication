import firebase from 'firebase'

//This is where we will make calls to fetch the users + Add users + Fetch posts

export function fetchUser(){ 
    return((dispatch) = {
        /*Call to get the 
        user 
        */
    })
}